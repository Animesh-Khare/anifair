import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  widgetPage: {
    marginTop: "15px",
    "& h6": {
      fontSize: 16,
      fontWeight: 700,
      lineHeight: 1.5,
    },
    "& h3": {
      fontSize: 18,
      fontWeight: 400,
      lineHeight: 1.5,
    },
  },
}));
